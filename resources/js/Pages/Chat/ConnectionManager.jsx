import React, { useEffect, useState } from "react";
import { Drawer } from "@mui/material";
import { IoMdMenu } from "react-icons/io";

function ConnectionManager({ id, connected, setConnected }) {
    return (
        <>
            {/* Tutaj dodaj elementy interfejsu użytkownika, które są związane z połączeniem WebSocket */}
        </>
    );
}

export default ConnectionManager;
